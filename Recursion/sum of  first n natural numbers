#include<bits/stdc++.h>
using namespace std;
int sum_natural(int n)
{
    if(n==0)
    return 0;
    
    return n+sum_natural(n-1);
}

int main()
{
    int n;
    cout<<"enter a number\n";
    cin>>n;
    
    cout<<"sum is -> "<<sum_natural(n);
    
    return 0;
}
